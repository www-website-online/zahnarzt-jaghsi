# Staged values only: merge after Google authorization and verified synchronization.
# Preserve the existing SMTP settings and both relay secrets.
MAIL_RELAY_GROUP=info@zahnarzt-jaghsi.de
MAIL_RELAY_GROUP_MEMBERS_FILE=/var/lib/zahnarzt-relay-members/members.json
MAIL_RELAY_GOOGLE_ADMIN=REPLACE_WITH_WORKSPACE_ADMIN_EMAIL
MAIL_RELAY_GOOGLE_CREDENTIALS=/etc/zahnarzt/group-directory-service-account.json
# Set MAIL_RELAY_ENABLED=1 only after the live group reply test succeeds.
